# Web dashboard (Flask)
flask>=3.0.0
werkzeug>=3.0.0
# System metrics (CPU/RAM/process)
psutil>=5.9.0
# NVIDIA GPU metrics via NVML
nvidia-ml-py>=12.0
# HTTP health checks and model discovery
requests>=2.31.0
# YAML configuration parsing
PyYAML>=6.0.0
